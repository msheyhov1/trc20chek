"""Общее для провайдеров.

Соглашение: провайдер НЕ глотает ошибки молча и НЕ возвращает пустой результат
вместо сбоя — он бросает ProviderError. Агрегатор ловит его в одном месте
(`_guarded`), пишет `provider_status[name] = "error"` и продолжает с тем, что есть.
Так «провайдер сказал чисто» и «провайдер недоступен» перестают быть неотличимы:
для AML-инструмента отсутствие данных не должно выглядеть как отсутствие риска.
"""
from __future__ import annotations


class ProviderError(Exception):
    """Провайдер не смог получить данные: сеть, HTTP-статус, невалидный ответ, лимит."""
